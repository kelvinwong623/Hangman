<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/color_background"
    tools:context="leafstudios.hangman.MainActivity"
    >

    <Button
        android:text="@string/hiName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:id="@+id/hiButton"
        android:layout_below="@+id/playButton"
        android:layout_alignStart="@+id/playButton"
        android:layout_alignEnd="@+id/playButton" />

    <Button
        android:text="Play"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/playButton"
        android:layout_marginTop="40dp"
        android:onClick ="playGame"
        android:layout_below="@+id/titleName"
        android:layout_alignEnd="@+id/titleName"
        android:layout_alignStart="@+id/titleName" />

    <TextView
        android:text="Hangman"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/titleName"
        android:textSize="50dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:textColor="@color/color_HangmanText"/>

</RelativeLayout>
